# fo-dicom.Codecs reference fixtures

These C# commands call the public fo-dicom.Codecs API only to create and
verify reference fixtures offline. The Java JPEG 2000 interoperability command
is in `tools/jpeg2000-interop`; the Java HTJ2K command is in
`tools/htj2k-interop`. Maven and CI never compile or run this directory.
